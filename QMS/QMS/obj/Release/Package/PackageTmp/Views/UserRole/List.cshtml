
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/Layout.cshtml";
    var subModules = Session["subModules"] != null ? (List<HRMS.Data.SubModule>)Session["subModules"] : new List<HRMS.Data.SubModule>();
    int subModuleId = subModules.Where(m => m.Name == "SystemUsers").FirstOrDefault().Id;
    bool canAdd = HRMS.Utilities.PermissionChecker.HasPermission(subModuleId, 2);
    bool canEdit = HRMS.Utilities.PermissionChecker.HasPermission(subModuleId, 3);
    bool canDelete = HRMS.Utilities.PermissionChecker.HasPermission(subModuleId, 4);
}

<main class="bg-white-500 flex-1 p-3 overflow-hidden">
    <div class="flex flex-col">
        <!--Grid Form-->
        <div class="flex flex-1  flex-col md:flex-row lg:flex-row mx-2">
            <div class="mb-2 border-solid border-gray-300 rounded border shadow-sm w-full">
                <div class="bg-gray-200 px-2 py-3 border-solid border-gray-200 border-b">
                    <span>User Roles</span>
                    @if (canAdd)
                    {
                        <span style="float:right;">
                            @*<button class="bg-green-500 hover:bg-green-800 text-white font-bold py-2 px-4 rounded" onclick="AddJobTitle();">
                                    Add Job Title
                                </button>*@
                            <a class="bg-green-500 hover:bg-green-800 text-white font-bold py-2 px-4 rounded text-decoration-none" href="@Url.Content("~/UserRole/Add")">
                                Add User Role
                            </a>

                        </span>
                    }
                </div>

                <div class="p-3">
                    <table class="table-responsive w-full text-12 table-striped">
                        <thead>
                            <tr>
                                <th class="border  px-24 py-2">User Name</th>
                                <th class="border  px-24 py-2">Role Assigned</th>
                                                                
                                @*<th class="border  px-24 py-2">Last Updated By/On</th>*@
                                <th class="border w-1/6 px-24 py-2">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (ViewBag.UserRoles != null && ViewBag.UserRoles.Count > 0)
                            {
                                foreach (var userRole in ViewBag.UserRoles)
                                {
                            <tr>
                                <td class="border ">@(userRole.SystemUser.LoginId)</td>
                                <td class="border ">@(userRole.Role.Name)</td>
                                
                                
                                @*<td class="border ">@(employee.Department.Name)</td>*@
                                @*<td class="border  ">@(userRole.UpdatedBy + " " + userRole.UpdatedOn)</td>*@
                                <td class="border ">

                                    @*@if (canEdit)
        {
            <a class="bg-teal-300 cursor-pointer rounded p-1 mx-1 text-white" href="@Url.Content("~/SystemUser/Edit/" + sysUser.Id)">
                <i class="fas fa-edit"></i>
            </a>      @if (canEdit)
                                        {
                                            <a class="bg-teal-300 cursor-pointer rounded p-1 mx-1 text-white" href="@Url.Content("~/JobTitle/Edit/" + jobTitle.Id)">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                        }
        }*@

                                    @if (canEdit)
                                    {
                                        <a class="bg-teal-300 cursor-pointer rounded p-1 mx-1 text-white" href="@Url.Content("~/UserRole/Edit/" + userRole.Id)">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    }
                                    @if (canDelete)
                                    {
                                        <a class="bg-teal-300 cursor-pointer rounded p-1 mx-1 text-red-500" href="@Url.Content("~/UserRole/Delete/" + userRole.Id)">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    }
                                </td>
                            </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="11" class="border px-4 py-4">
                                        No Record Found!
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <!--/Grid Form-->
    </div>
</main>

